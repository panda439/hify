// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.27.0
// source: chunks.sql

package gen

import (
	"context"
	"encoding/json"
)

const countChunksByKnowledgeBase = `-- name: CountChunksByKnowledgeBase :one
SELECT COUNT(*) FROM chunks WHERE knowledge_base_id = ?
`

func (q *Queries) CountChunksByKnowledgeBase(ctx context.Context, knowledgeBaseID string) (int64, error) {
	row := q.db.QueryRowContext(ctx, countChunksByKnowledgeBase, knowledgeBaseID)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const createChunk = `-- name: CreateChunk :exec
INSERT INTO chunks (
    id, knowledge_base_id, document_id, chunk_index, content,
    content_length, embedding, embedding_dimension
) VALUES (?, ?, ?, ?, ?, ?, ?, ?)
`

type CreateChunkParams struct {
	ID                 string          `json:"id"`
	KnowledgeBaseID    string          `json:"knowledge_base_id"`
	DocumentID         string          `json:"document_id"`
	ChunkIndex         int32           `json:"chunk_index"`
	Content            string          `json:"content"`
	ContentLength      int32           `json:"content_length"`
	Embedding          json.RawMessage `json:"embedding"`
	EmbeddingDimension int32           `json:"embedding_dimension"`
}

func (q *Queries) CreateChunk(ctx context.Context, arg CreateChunkParams) error {
	_, err := q.db.ExecContext(ctx, createChunk,
		arg.ID,
		arg.KnowledgeBaseID,
		arg.DocumentID,
		arg.ChunkIndex,
		arg.Content,
		arg.ContentLength,
		arg.Embedding,
		arg.EmbeddingDimension,
	)
	return err
}

const deleteChunksByDocument = `-- name: DeleteChunksByDocument :exec
DELETE FROM chunks WHERE document_id = ?
`

func (q *Queries) DeleteChunksByDocument(ctx context.Context, documentID string) error {
	_, err := q.db.ExecContext(ctx, deleteChunksByDocument, documentID)
	return err
}

const listChunksByKnowledgeBase = `-- name: ListChunksByKnowledgeBase :many
SELECT id, knowledge_base_id, document_id, chunk_index, content,
       content_length, embedding, embedding_dimension, created_at
FROM chunks
WHERE knowledge_base_id = ?
ORDER BY document_id, chunk_index
`

// Used to (re)build the per-knowledge-base embedding-matrix cache — always
// bounded by knowledge_base_id per CLAUDE.md's large-table rule, no LIMIT
// since the caller wants the full set (soft-capped at upload time, not
// read time).
func (q *Queries) ListChunksByKnowledgeBase(ctx context.Context, knowledgeBaseID string) ([]Chunk, error) {
	rows, err := q.db.QueryContext(ctx, listChunksByKnowledgeBase, knowledgeBaseID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Chunk{}
	for rows.Next() {
		var i Chunk
		if err := rows.Scan(
			&i.ID,
			&i.KnowledgeBaseID,
			&i.DocumentID,
			&i.ChunkIndex,
			&i.Content,
			&i.ContentLength,
			&i.Embedding,
			&i.EmbeddingDimension,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
